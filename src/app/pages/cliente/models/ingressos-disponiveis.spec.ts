import { IngressosDisponiveis } from './ingressos-disponiveis';

describe('IngressosDisponiveis', () => {
  it('should create an instance', () => {
    expect(new IngressosDisponiveis()).toBeTruthy();
  });
});
